
            int main(int array[]) {
                int max,i,length;
                max = array[0];
                for (i; i < length; i=i+1) {
                    if (max < array[i])
                        max = array[i];
                }
                putInt(max);
            }
        